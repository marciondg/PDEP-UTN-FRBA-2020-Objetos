import mensajeriaWollokiana.*
import chats.*
import contenidos.*

describe "Tests Parcial POO"{
	const marcio = new Usuario(nombre="Marcio Garozzo", chats=#{chat})
	const chat = new Chat(participantes=[marcio])
test "Se " {

	assert.that(false)

}
}